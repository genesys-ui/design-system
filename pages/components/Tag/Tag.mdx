import { Meta, Source } from '@storybook/blocks';

import * as Stories from './Tag.stories';

import { DesignTokensList, LiveDemo } from '../../../blocks';

<Meta title="Components/Tag" />

# Tag

(PENDING)

## Overview

(PENDING) Variants, sizes, alignment, location... etc.

## Live demo

<LiveDemo
  story={Stories.Example}
  urlHtml="https://devoinc.github.io/genesys-styles/?path=/docs/components-tag--docs"
  urlReact="https://devoinc.github.io/genesys-ui/?path=/docs/components-feedback-tag--docs"
/>

## Anatomy

(PENDING)

## Accessibility

### aria-label

We can use the `aria-label` attribute to inform about what the Tag is categorizing or grouping
to assistive technologies as screen readers.

<Source
  code={`
  <span class="tag" aria-label="Category of the article: Design tokens">
    Design tokens
  </span>
  `}
/>

## Design tokens

(PENDING)

The component uses alias tokens for typography.

⚠️ It is necessary to create the rest of specific component tokens, or at least add the alias tokens to the list.

### Complete list

<DesignTokensList componentName="tag" />
