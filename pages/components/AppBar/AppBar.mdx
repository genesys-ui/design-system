import { Meta, Canvas, Controls, Source } from '@storybook/blocks';

import * as Stories from './AppBar.stories';

import { CardHtml, CardReact, DesignTokensList, Figma, Figure, } from '../../../blocks';
import { getCmpTokensJson } from '../../../tokens';

<Meta of={Stories} />


# App bar

(PENDING)

## Overview

(PENDING) Variants, sizes, alignment, location... etc.

## Live demo

Preview the AppBar component with the React live demo. For detailed code usage
documentation, see the Genesys UI or Genesys Styles version of the component.

<Canvas of={Stories.Example} />
<Controls of={Stories.Example} />

<section className="mrow">
  <div className="mcol-12 mcol-sm-6">
    <CardReact url="https://devoinc.github.io/genesys-ui/?path=/docs/components-core-navigation-appbar--docs" />
  </div>
  <div className="mcol-12 mcol-sm-6">
    <CardHtml url="https://gitlab.devotools.com/engineering/quvis/genesys-styles" />
  </div>
</section>

## Anatomy
The App Bar is compoused by three areas:
- On the left is the main page/section name.
- The next area o the left, separated with a divider, is the main navigation with the subsections of the page.
- On the right of the App Bar there are some main actions and the user options separated with another divider.
<Figure caption="The internal structure and specs of the IconButton component.">
  <Figma url="https://www.figma.com/design/4f9wa0fKYIwMue6yo6nrJL/GDS---Web-Previews?node-id=14132-64784&t=RQb9FPEqsJrvQIC5-1" />

</Figure>

## Accessibility

(PENDING)

## Design tokens

(PENDING)

### Complete list

(PENDING)

⚠️ It is necessary to check if there are component tokens to create the rest of specific component tokens, or at least add the alias tokens to the list.

<DesignTokensList
  tokens={getCmpTokensJson('appBar')}
  intro={
    <p style={{ margin: '1.6rem 0' }}>
      There are {getCmpTokensJson('appBar').length} AppBar design tokens. The
      units in rem are not based in browser, but in base 10, so{' '}
      <code>1rem = 10px</code>.
    </p>
  }
/>
